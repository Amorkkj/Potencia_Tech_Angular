const {gets, print} = require('./funcoes_auxiliares');

print(gets());

/*
//destruir objeto

const pessoa = {
    nome: 'vitor'
};

// ambos são a mesma coisa.
//const nome = pessoa.nome
const { nome } = pessoa;
*/